SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER   PROCEDURE [dbo].[TMS_DeleteTask_20250508]
    @tId INT
AS
BEGIN
    SET NOCOUNT ON;
    IF (( SELECT COUNT(Id)  FROM TMS_tbl_Task_20250508 WHERE Id = @tId ) > 0 )
        BEGIN 
            DELETE FROM TMS_tbl_Task_20250508 WHERE Id = @tId;
        END
    ELSE
    RAISERROR ('This Id doesn not exist ',16,1)
    RETURN 0;
END
GO
